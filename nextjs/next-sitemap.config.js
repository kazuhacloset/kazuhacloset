/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://www.kazuhacloset.com/", // replace with your real domain
  generateRobotsTxt: true, // also generates robots.txt
  sitemapSize: 7000,
  generateIndexSitemap: true,
};

module.exports = config;
